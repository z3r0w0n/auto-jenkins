# Playbook to configure and deploy servers

- hosts:
  - wservers
  become: yes
  become_method: sudo
  roles:
    - web-config

- hosts:
  - jenkins
  become: yes
  become_method: sudo
  roles:
    - jenkins-docker
